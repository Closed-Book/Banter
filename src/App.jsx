// ⚠️ 占位文件 —— P1（前端主力）将用真实的五屏路由替换本文件。
// 保留它只是为了让 `npm run build` 在脚手架阶段就能通过。
// P1 的目标结构见 docs/任务卡/P1-前端.md。
export default function App() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'system-ui', color: '#333' }}>
      <h1>别把天聊死</h1>
      <p>脚手架就绪，等待 P1 实现五屏。</p>
    </div>
  );
}
